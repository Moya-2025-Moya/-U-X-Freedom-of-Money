# Copy to .env.local and fill in. Never commit real values.

# Password gate for /freedomofmoney/admin
ADMIN_PASSWORD=change-me

# Optional: dedicated BSC RPC endpoint. Falls back to public node if unset.
# BSC_RPC_URL=
