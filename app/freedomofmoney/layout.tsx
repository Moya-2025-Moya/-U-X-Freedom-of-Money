// Route-segment layout for /freedomofmoney/*. Having a layout here lets
// error.tsx / loading.tsx / not-found.tsx scope to this feature.
export default function FreedomOfMoneyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
